{
  "name": "gateway",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "install": "cd actividades && npm install && cd ../notificaciones && npm install && cd ../gateway && npm install && cd ../reportes && npm install && cd ../suscripciones && npm install && cd ../usuarios && npm install",
    "start": "node index.js",
    "dev": "npx nodemon index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.7.8",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.1",
    "gateway": "file:",
    "morgan": "^1.10.0",
    "node-cron": "^3.0.3"
  }
}
